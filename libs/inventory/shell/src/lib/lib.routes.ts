import { Route } from '@angular/router';
import { InventoryShell } from './inventory-shell/inventory-shell';

export const inventoryShellRoutes: Route[] = [
  { path: '', component: InventoryShell },
];
