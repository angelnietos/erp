import { Component } from '@angular/core';

/**
 * Componente mínimo para la ruta `bot`: el guard redirige antes de pintar nada.
 */
@Component({
  standalone: true,
  template: '',
})
export class AssistantBotStubComponent {}
